import loaderImageMap from "./components/loaderImageMap";

const loaderScreenMap = {
  tag: "div",
  attrs: {
    id: "loader-screen",
  },
  children: loaderImageMap,
};

export default loaderScreenMap;
